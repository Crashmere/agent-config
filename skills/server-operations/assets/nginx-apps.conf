# 共享 HTTP 入口模板，仅首次配置空服务器时安装。
server {
    listen 80 default_server;
    listen [::]:80 default_server;
    server_name _;
    server_tokens off;

    include /etc/nginx/app-locations/*.conf;
    location / { return 404; }
}
